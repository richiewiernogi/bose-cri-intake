# Copy this into Streamlit Cloud → App Settings → Secrets
# (Do NOT rename to secrets.toml and commit — it's in .gitignore)

GMAIL_ADDRESS = "e.wiernasz@gmail.com"
GMAIL_APP_PASSWORD = "kawyvmyblhrvtltw"
CRI_RECIPIENT = "erich_wiernasz@bose.com"

# Optional: pre-set the Anthropic key so users don't need to enter it
# ANTHROPIC_API_KEY = "sk-ant-..."
